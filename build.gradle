plugins {
	id 'java'
	id 'org.springframework.boot' version '3.3.4'   		// ✅ 다운그레이드 (3.5.7 → 3.3.4)
	id 'io.spring.dependency-management' version '1.1.6'  	// ✅ 3.3.x에 맞는 안정 버전
}

group = 'com.habitsnap'
version = '0.0.1-SNAPSHOT'

java {
	sourceCompatibility = '17'   // 21도 가능하지만 JPA 호환성 위해 17 유지
}

repositories {
	mavenCentral()
}

dependencies {
	// ✅ Spring Boot 기본 웹 및 DB
	implementation 'org.springframework.boot:spring-boot-starter-web'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.springframework.boot:spring-boot-starter-security'
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	runtimeOnly 'com.mysql:mysql-connector-j'   // ✅ 수정된 부분

	// ✅ Swagger(OpenAPI) - API 문서 자동화
	implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.6.0'

	// ✅ Lombok (보일러플레이트 제거)
	compileOnly 'org.projectlombok:lombok'
	annotationProcessor 'org.projectlombok:lombok'

	// ✅ 개발 편의성 - DevTools (자동 리로드 등)
	developmentOnly 'org.springframework.boot:spring-boot-devtools'

	// ✅ 테스트 환경
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'

	// ✅ AssertJ 최신 버전 (기본 포함된 3.24.x 대신 이 버전이 우선 적용됨)
	testImplementation 'org.assertj:assertj-core:3.26.3'

	// 🔐 JWT (io.jsonwebtoken)
	implementation 'io.jsonwebtoken:jjwt-api:0.11.5'
	runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.11.5'
	runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.11.5' // JSON 파싱을 위한 Jackson 기반 모듈

	// EXIF 라이브러리
	implementation 'com.drewnoakes:metadata-extractor:2.18.0'

	// AWS SDK
	implementation 'software.amazon.awssdk:s3:2.25.60'

}

tasks.named('test') {
	useJUnitPlatform()
}
